<html lang='en'>
  <head>
    <meta charset='utf-8' />
    <meta name='viewport' content='width=device-width,initial-scale=1' />
    <title>Password reset Token</title>
    <style>
      :root {
        --primary: #1e88e5;
        --primary-dark: #1565c0;
        --bg: #f7f9fc;
        --muted: #6b7280;
      }
      body {
        font-family: Arial, Helvetica, sans-serif;
        background: var(--bg);
        margin: 0;
        padding: 0;
        color: #222;
      }
      .container {
        max-width: 600px;
        margin: 28px auto;
        background: #fff;
        border-radius: 8px;
        padding: 28px;
        box-shadow: 0 2px 6px rgba(0, 0, 0, 0.06);
        border-top: 6px solid var(--primary);
      }
      h1 {
        font-size: 20px;
        margin: 0 0 8px;
        color: var(--primary-dark);
      }
      p {
        margin: 12px 0;
        line-height: 1.5;
      }
      .btn {
        display: inline-block;
        background: linear-gradient(
          180deg,
          var(--primary),
          var(--primary-dark)
        );
        color: #fff;
        padding: 12px 18px;
        border-radius: 6px;
        text-decoration: none;
      }
      .muted {
        color: var(--muted);
        font-size: 13px;
      }
      .code {
        background: #f3f4f6;
        padding: 8px 10px;
        border-radius: 4px;
        font-family: monospace;
        word-break: break-all;
      }
      .footer {
        margin-top: 20px;
        font-size: 13px;
        color: #8a8a8a;
      }
      @media (max-width: 520px) {
        .container {
          padding: 18px;
        }
        .btn {
          padding: 10px 14px;
        }
      }
    </style>
  </head>
  <body>
    <div class='container'>
      <h1>Password reset token</h1>

      <p>Hello {{name}},</p>

      <p>We received a request to reset the password for your
        <strong>{{appName}}</strong>
        account.</p>

      <p class='muted'>Below is your password reset token. Copy it and paste it
        into the app to continue.</p>

      {{#if token}}
        <p style='text-align:center;margin:18px 0;'>
          <span
            class='code'
            style='display:inline-block;padding:14px 18px;font-size:16px;color:#0b2545'
          >{{token}}</span>
        </p>
        <p class='muted'>This token expires in
          <strong>{{expiresIn}}</strong>.</p>
      {{else}}
        <p class='muted'>No token was provided. If you believe this is an error,
          contact
          <a href='mailto:{{supportEmail}}'>{{supportEmail}}</a>.</p>
      {{/if}}

      <p>If you didn't request a password reset, please ignore this email or
        contact support at
        <a href='mailto:{{supportEmail}}'>{{supportEmail}}</a>.</p>

      <div class='footer'>— The {{appName}} team</div>
    </div>
  </body>
</html>