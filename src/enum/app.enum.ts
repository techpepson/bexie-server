export enum Role {
  VENDOR = 'vendor',
  SYSTEM_ADMIN = 'system_admin',
  ADMIN = 'admin',
  MERCHANT = 'merchant',
  RIDER = 'rider',
}

export enum Status {
  ACTIVE = 'active',
  SUSPENDED = 'suspended',
  PENDING = 'pending',
}

export enum PasswordResetChannel {
  EMAIL = 'email',
  SMS = 'sms',
}
